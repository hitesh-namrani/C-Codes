#include<stdio.h>
#include<conio.h>
int main()
{
    int length,breadth;
    printf("Enter length");
    scanf("%d", &length);
    printf("Enter breadth");
    scanf("%d",&breadth);
    printf("the area of rectangle is :%d",length*breadth);
    printf("sq units");
    getch();
    return 0;
}